# RaaS — dependencies (Phase 1 + Phase 2).
# NOTE: torch is installed separately in the Dockerfile from the CPU-only index
# (the default PyPI wheel bundles CUDA and would triple the image size).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0

# LLM provider SDKs (adapter layer)
openai>=1.40,<2.0
google-genai>=1.0,<2.0

httpx>=0.27,<1.0
python-multipart>=0.0.9

# Phase 2 — ingestion + retrieval
qdrant-client>=1.12,<2.0
sentence-transformers>=3.0,<6.0
pypdf>=4.0,<7.0
asyncpg>=0.29,<1.0

# Phase 3 — agentic loop
langgraph>=0.2,<2.0

# Phase 4 — observability (SDK v2 matches the self-hosted Langfuse v2 server)
langfuse>=2.50,<3.0
